pages:
  image: ubuntu:noble
  script:
    - export DEBIAN_FRONTEND=noninteractive
    - apt-get update
    - apt-get install -y unzip python3 python3-jinja2 curl git
    - ./pages.sh
  artifacts:
    paths:
      - public
  only:
    - website
